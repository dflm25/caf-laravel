import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  planContainer: {
    padding: 15,
    marginVertical: 20,
  },
  sectionTitle: {
    fontSize: 20,
    fontWeight: "600",
  },
});

export default styles;
